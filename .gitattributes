*.onnx filter=lfs diff=lfs merge=lfs -text
# CoreML MI-GAN model weights (14 MB) — the .mlpackage's small Manifest.json +
# model.mlmodel stay in plain git; only the weight blob goes through LFS.
assets/migan_512_places2_fp16.mlpackage/Data/com.apple.CoreML/weights/weight.bin filter=lfs diff=lfs merge=lfs -text
